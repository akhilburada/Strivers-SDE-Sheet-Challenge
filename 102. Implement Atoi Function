
#from os import *
from sys import *
from collections import *
from math import *

def atoi(str):
    print(str)
    return ''
    res = ''
    for ch in str:
        if(ch.isalpha()):
            continue
        if(ch.isdigit()):
            res += ch
    return res

    
