def median(arr1, arr2):
    arr1.extend(arr2)
    median=0
    arr1.sort()
    mid=len(arr1)//2
    if len(arr1)%2!=0:
        median=arr1[mid]
    else:
       
        median=(arr1[mid]+arr1[mid-1])/2
    return float(median)
